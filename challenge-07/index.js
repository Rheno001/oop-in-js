//taking two things hasteaching assistant which is a boolean and classlist(first value is teacher)

const classRoom = {
    hasTeachingAssistant :true,
    classlist : ['Mrs Julie', 'John', 'Mary']
};

function getStudents(classRoom){
    if(hasTeachingAssistant === true){
        return true
    }else return false;
    

};